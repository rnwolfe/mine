---
import Landing from "../layouts/Landing.astro";
---

<Landing>
  <!-- Navigation -->
  <header class="site-nav">
    <div class="nav-inner">
      <a href="/" class="nav-logo" aria-label="mine home">
        <span class="logo-icon">⛏️</span>
        <span class="logo-text">mine</span>
      </a>
      <nav class="nav-links" aria-label="Main navigation">
        <a href="/docs/" class="nav-link">Docs</a>
        <a href="/docs/commands/todo/" class="nav-link">Commands</a>
        <a
          href="https://github.com/rnwolfe/mine"
          class="nav-link"
          target="_blank"
          rel="noopener"
        >
          GitHub
        </a>
      </nav>
      <a href="/docs/getting-started/installation/" class="btn btn-primary nav-cta"
        >Get Started</a
      >
    </div>
  </header>

  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-bg-grid" aria-hidden="true"></div>
      <div class="hero-bg-glow" aria-hidden="true"></div>
      <div class="section-inner hero-inner">
        <div class="hero-badge">
          <span class="badge-dot"></span>
          Single binary. Zero dependencies.
        </div>
        <h1 class="hero-title">
          Your developer CLI,<br />
          <span class="hero-title-accent">supercharged.</span>
        </h1>
        <p class="hero-description">
          <strong>mine</strong> replaces the sprawl of productivity tools with one fast, delightful,
          opinionated Go binary. Tasks, focus sessions, scaffolding, dotfiles — all in under 50ms.
        </p>
        <div class="hero-install">
          <div class="install-box">
            <code class="install-cmd">curl -fsSL https://mine.rwolfe.io/install | bash</code>
            <button
              class="install-copy"
              aria-label="Copy install command"
              data-copy="curl -fsSL https://mine.rwolfe.io/install | bash"
            >
              <svg
                width="16"
                height="16"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                aria-hidden="true"
              >
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                <path
                  d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"
                ></path>
              </svg>
            </button>
          </div>
          <span class="install-note">macOS & Linux · x86_64 & arm64</span>
        </div>
        <div class="hero-actions">
          <a href="/docs/getting-started/installation/" class="btn btn-primary btn-lg">
            Get Started
          </a>
          <a
            href="https://github.com/rnwolfe/mine"
            class="btn btn-ghost btn-lg"
            target="_blank"
            rel="noopener"
          >
            <svg
              width="18"
              height="18"
              viewBox="0 0 24 24"
              fill="currentColor"
              aria-hidden="true"
            >
              <path
                d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0 1 12 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"
              ></path>
            </svg>
            View on GitHub
          </a>
        </div>
      </div>
    </section>

    <!-- Quick Demo Section -->
    <section class="demo-section section">
      <div class="section-inner">
        <div class="terminal">
          <div class="terminal-header" aria-hidden="true">
            <span class="terminal-dot terminal-dot-red"></span>
            <span class="terminal-dot terminal-dot-yellow"></span>
            <span class="terminal-dot terminal-dot-green"></span>
            <span class="terminal-title">Terminal</span>
          </div>
          <pre class="terminal-body"><code><span class="term-comment"># initialize mine for the first time</span>
<span class="term-prompt">$</span> <span class="term-cmd">mine init</span>
<span class="term-out">⛏️  Welcome to mine! Configuration saved to ~/.config/mine/config.toml</span>

<span class="term-comment"># your personal dashboard</span>
<span class="term-prompt">$</span> <span class="term-cmd">mine</span>
<span class="term-out">┌──────────────────────────────────┐
│  mine  ·  your dev supercharger  │
├──────────────────────────────────┤
│  📋  3 tasks · 1 high priority   │
│  ⏱   Last dig: 2h 30m today      │
│  📦  2 plugins installed         │
└──────────────────────────────────┘</span>

<span class="term-comment"># add a task with priority and due date</span>
<span class="term-prompt">$</span> <span class="term-cmd">mine todo add "ship v0.2" -p high -d tomorrow</span>
<span class="term-out">✓  Added: ship v0.2  [high] [due: tomorrow]</span>

<span class="term-comment"># start a 25-minute focus session</span>
<span class="term-prompt">$</span> <span class="term-cmd">mine dig 25m</span>
<span class="term-out">🎯 Focus started · 25:00 remaining · press q to quit</span>

<span class="term-comment"># scaffold a new Go project</span>
<span class="term-prompt">$</span> <span class="term-cmd">mine craft dev go</span>
<span class="term-out">✓  Scaffolded Go project in ./myapp</span></code></pre>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="features-section section">
      <div class="section-inner">
        <div class="section-header">
          <h2 class="section-title">Everything in one place.</h2>
          <p class="section-subtitle">
            Stop juggling tools. mine handles the workflow so you stay in flow.
          </p>
        </div>
        <div class="features-grid">
          <div class="feature-card">
            <div class="feature-icon" aria-hidden="true">📋</div>
            <h3 class="feature-title">Tasks</h3>
            <p class="feature-desc">
              Fast task management with priorities, due dates, and tags. No app to open — just
              <code>mine todo</code>.
            </p>
            <a href="/docs/commands/todo/" class="feature-link">mine todo →</a>
          </div>
          <div class="feature-card">
            <div class="feature-icon" aria-hidden="true">⏱</div>
            <h3 class="feature-title">Focus</h3>
            <p class="feature-desc">
              Pomodoro timer with streak tracking and deep work stats. Every session counted, every
              streak earned.
            </p>
            <a href="/docs/commands/dig/" class="feature-link">mine dig →</a>
          </div>
          <div class="feature-card">
            <div class="feature-icon" aria-hidden="true">🚀</div>
            <h3 class="feature-title">Scaffold</h3>
            <p class="feature-desc">
              Bootstrap Go, Node, Python, Rust projects in seconds with built-in recipes. Add your
              own templates.
            </p>
            <a href="/docs/commands/craft/" class="feature-link">mine craft →</a>
          </div>
          <div class="feature-card">
            <div class="feature-icon" aria-hidden="true">🗂</div>
            <h3 class="feature-title">Dotfiles</h3>
            <p class="feature-desc">
              Track, diff, and sync your config files across machines. Git-backed versioning built
              in.
            </p>
            <a href="/docs/commands/stash/" class="feature-link">mine stash →</a>
          </div>
          <div class="feature-card">
            <div class="feature-icon" aria-hidden="true">🐚</div>
            <h3 class="feature-title">Shell</h3>
            <p class="feature-desc">
              Completions for bash, zsh, and fish. Handy aliases that just work out of the box.
            </p>
            <a href="/docs/commands/shell/" class="feature-link">mine shell →</a>
          </div>
          <div class="feature-card feature-card-accent">
            <div class="feature-icon" aria-hidden="true">⚡</div>
            <h3 class="feature-title">Blazing Fast</h3>
            <p class="feature-desc">
              Every local command responds in under 50ms. Single static binary, zero runtime
              dependencies, curl-installable.
            </p>
            <span class="feature-stat">&lt; 50ms</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Philosophy Section -->
    <section class="philosophy-section section">
      <div class="section-inner philosophy-inner">
        <div class="philosophy-text">
          <h2 class="section-title">Built on principles.</h2>
          <p class="philosophy-lead">
            mine is opinionated by design. Good defaults mean you spend your time building, not
            configuring.
          </p>
          <ul class="philosophy-list">
            <li class="philosophy-item">
              <span class="philosophy-icon" aria-hidden="true">⚡</span>
              <div>
                <strong>Speed first</strong>
                <span>Every local command under 50ms. No spinners for local ops.</span>
              </div>
            </li>
            <li class="philosophy-item">
              <span class="philosophy-icon" aria-hidden="true">📦</span>
              <div>
                <strong>Single binary</strong>
                <span>No runtime dependencies. <code>curl | sh</code> and you're done.</span>
              </div>
            </li>
            <li class="philosophy-item">
              <span class="philosophy-icon" aria-hidden="true">🎯</span>
              <div>
                <strong>Opinionated defaults</strong>
                <span>Works out of the box. Escape hatches exist when you need them.</span>
              </div>
            </li>
            <li class="philosophy-item">
              <span class="philosophy-icon" aria-hidden="true">🔒</span>
              <div>
                <strong>Local first</strong>
                <span>Your data stays on your machine. Cloud features are opt-in.</span>
              </div>
            </li>
            <li class="philosophy-item">
              <span class="philosophy-icon" aria-hidden="true">📁</span>
              <div>
                <strong>XDG-compliant</strong>
                <span
                  >Config in <code>~/.config/mine/</code>, data in <code
                    >~/.local/share/mine/</code
                  >.</span
                >
              </div>
            </li>
          </ul>
        </div>
        <div class="philosophy-aside">
          <div class="stats-card">
            <div class="stat-item">
              <span class="stat-value">~7.6MB</span>
              <span class="stat-label">Binary size (stripped)</span>
            </div>
            <div class="stat-divider" aria-hidden="true"></div>
            <div class="stat-item">
              <span class="stat-value">&lt; 50ms</span>
              <span class="stat-label">Every local command</span>
            </div>
            <div class="stat-divider" aria-hidden="true"></div>
            <div class="stat-item">
              <span class="stat-value">0</span>
              <span class="stat-label">Runtime dependencies</span>
            </div>
            <div class="stat-divider" aria-hidden="true"></div>
            <div class="stat-item">
              <span class="stat-value">MIT</span>
              <span class="stat-label">Open source license</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section section">
      <div class="section-inner">
        <div class="cta-box">
          <div class="cta-glow" aria-hidden="true"></div>
          <h2 class="cta-title">Ready to supercharge your workflow?</h2>
          <p class="cta-desc">
            One command to install. Start in seconds. Your productivity stack, unified.
          </p>
          <div class="cta-install">
            <div class="install-box">
              <code class="install-cmd">curl -fsSL https://mine.rwolfe.io/install | bash</code>
              <button
                class="install-copy"
                aria-label="Copy install command"
                data-copy="curl -fsSL https://mine.rwolfe.io/install | bash"
              >
                <svg
                  width="16"
                  height="16"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  aria-hidden="true"
                >
                  <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                  <path
                    d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"
                  ></path>
                </svg>
              </button>
            </div>
          </div>
          <div class="cta-actions">
            <a href="/docs/getting-started/installation/" class="btn btn-primary btn-lg">
              Read the Docs
            </a>
            <a
              href="https://github.com/rnwolfe/mine"
              class="btn btn-ghost btn-lg"
              target="_blank"
              rel="noopener"
            >
              Star on GitHub
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer class="site-footer">
    <div class="footer-inner">
      <div class="footer-brand">
        <span class="footer-logo-icon" aria-hidden="true">⛏️</span>
        <span class="footer-logo-text">mine</span>
      </div>
      <nav class="footer-links" aria-label="Footer navigation">
        <a href="/docs/" class="footer-link">Documentation</a>
        <a href="/docs/commands/todo/" class="footer-link">Commands</a>
        <a href="/docs/getting-started/installation/" class="footer-link">Install</a>
        <a href="https://github.com/rnwolfe/mine" class="footer-link" target="_blank" rel="noopener"
          >GitHub</a
        >
      </nav>
      <p class="footer-copy">
        Built by <a href="https://github.com/rnwolfe" target="_blank" rel="noopener">Ryan Wolfe</a>.
        MIT License.
      </p>
    </div>
  </footer>
</Landing>

<style>
  /* ============================================
     LAYOUT UTILITIES
     ============================================ */
  .section {
    padding: 5rem 0;
  }

  .section-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* ============================================
     NAVIGATION
     ============================================ */
  .site-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: rgba(13, 13, 13, 0.85);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--color-border-subtle);
  }

  .nav-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 64px;
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
    font-size: 1.125rem;
    color: var(--color-light);
    text-decoration: none;
    flex-shrink: 0;
  }

  .logo-icon {
    font-size: 1.25rem;
  }

  .logo-text {
    font-family: var(--font-mono);
    letter-spacing: -0.5px;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    margin-left: auto;
  }

  .nav-link {
    padding: 0.375rem 0.75rem;
    border-radius: 6px;
    font-size: 0.9rem;
    color: var(--color-text);
    transition:
      color 0.15s,
      background-color 0.15s;
  }

  .nav-link:hover {
    color: var(--color-light);
    background-color: rgba(255, 255, 255, 0.06);
  }

  .nav-cta {
    margin-left: 0.5rem;
    flex-shrink: 0;
  }

  @media (max-width: 640px) {
    .nav-links {
      display: none;
    }
  }

  /* ============================================
     BUTTONS
     ============================================ */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.5rem 1.25rem;
    border-radius: 8px;
    font-weight: 500;
    font-size: 0.9rem;
    text-decoration: none;
    transition:
      opacity 0.15s,
      transform 0.15s,
      box-shadow 0.15s;
    cursor: pointer;
    border: none;
    white-space: nowrap;
  }

  .btn:hover {
    transform: translateY(-1px);
  }

  .btn:active {
    transform: translateY(0);
  }

  .btn-primary {
    background: var(--gradient-primary);
    color: #0d0d0d;
    font-weight: 600;
    box-shadow: 0 0 20px rgba(212, 160, 23, 0.25);
  }

  .btn-primary:hover {
    box-shadow: 0 0 30px rgba(212, 160, 23, 0.4);
  }

  .btn-ghost {
    background: transparent;
    color: var(--color-text);
    border: 1px solid var(--color-border-subtle);
  }

  .btn-ghost:hover {
    color: var(--color-light);
    border-color: var(--color-border);
    background: rgba(255, 255, 255, 0.04);
  }

  .btn-lg {
    padding: 0.75rem 1.75rem;
    font-size: 1rem;
    border-radius: 10px;
  }

  /* ============================================
     HERO
     ============================================ */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding-top: 64px;
  }

  .hero-bg-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(212, 160, 23, 0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(212, 160, 23, 0.04) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 40%, transparent 100%);
    -webkit-mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 40%, transparent 100%);
  }

  .hero-bg-glow {
    position: absolute;
    top: 20%;
    left: 50%;
    transform: translateX(-50%);
    width: 800px;
    height: 400px;
    background: radial-gradient(ellipse, rgba(212, 160, 23, 0.12) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-inner {
    padding-top: 4rem;
    padding-bottom: 5rem;
    text-align: center;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.375rem 0.875rem;
    background: rgba(212, 160, 23, 0.1);
    border: 1px solid rgba(212, 160, 23, 0.25);
    border-radius: 100px;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--color-primary-light);
    letter-spacing: 0.02em;
    margin-bottom: 1.75rem;
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-primary);
    box-shadow: 0 0 6px var(--color-primary);
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.4;
    }
  }

  .hero-title {
    font-size: clamp(2.75rem, 6vw, 5rem);
    font-weight: 700;
    color: var(--color-light);
    line-height: 1.1;
    letter-spacing: -2px;
    margin-bottom: 1.5rem;
  }

  .hero-title-accent {
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-description {
    font-size: 1.125rem;
    color: var(--color-text);
    max-width: 560px;
    margin: 0 auto 2.5rem;
    line-height: 1.7;
  }

  .hero-description strong {
    color: var(--color-light);
    font-weight: 600;
  }

  .hero-install {
    margin-bottom: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .install-note {
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .hero-actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* ============================================
     INSTALL BOX
     ============================================ */
  .install-box {
    display: flex;
    align-items: center;
    gap: 0;
    background: var(--color-dark-2);
    border: 1px solid var(--color-border);
    border-radius: 10px;
    padding: 0.75rem 1rem;
    max-width: 500px;
    width: 100%;
  }

  .install-cmd {
    flex: 1;
    font-size: 0.85rem;
    color: var(--color-primary-light);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .install-copy {
    background: none;
    border: none;
    cursor: pointer;
    color: var(--color-text-muted);
    padding: 0.25rem;
    border-radius: 4px;
    display: flex;
    align-items: center;
    transition: color 0.15s;
    flex-shrink: 0;
    margin-left: 0.5rem;
  }

  .install-copy:hover {
    color: var(--color-primary);
  }

  .install-copy.copied {
    color: #4ade80;
  }

  /* ============================================
     TERMINAL DEMO
     ============================================ */
  .demo-section {
    background: var(--color-dark);
    border-top: 1px solid var(--color-border-subtle);
    border-bottom: 1px solid var(--color-border-subtle);
  }

  .terminal {
    background: #0a0a0a;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    overflow: hidden;
    box-shadow:
      0 24px 48px rgba(0, 0, 0, 0.6),
      0 0 0 1px rgba(255, 255, 255, 0.04);
    max-width: 760px;
    margin: 0 auto;
  }

  .terminal-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1rem;
    background: rgba(255, 255, 255, 0.03);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  .terminal-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .terminal-dot-red {
    background: #ff5f57;
  }

  .terminal-dot-yellow {
    background: #ffbd2e;
  }

  .terminal-dot-green {
    background: #28c840;
  }

  .terminal-title {
    margin-left: auto;
    margin-right: auto;
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.3);
    font-family: var(--font-mono);
  }

  .terminal-body {
    padding: 1.5rem;
    overflow-x: auto;
  }

  .terminal-body code {
    display: block;
    font-size: 0.82rem;
    line-height: 1.8;
    color: var(--color-text);
    white-space: pre;
  }

  .term-comment {
    color: #4a5568;
  }

  .term-prompt {
    color: var(--color-primary);
    font-weight: 600;
    user-select: none;
  }

  .term-cmd {
    color: var(--color-light);
  }

  .term-out {
    color: #718096;
  }

  /* ============================================
     FEATURES
     ============================================ */
  .features-section {
    background: var(--color-body);
  }

  .section-header {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  .section-title {
    font-size: clamp(2rem, 4vw, 3.25rem);
    font-weight: 700;
    color: var(--color-light);
    letter-spacing: -1.5px;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .section-subtitle {
    font-size: 1.0625rem;
    color: var(--color-text);
    max-width: 480px;
    margin: 0 auto;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  @media (max-width: 900px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 560px) {
    .features-grid {
      grid-template-columns: 1fr;
    }
  }

  .feature-card {
    background: var(--color-dark);
    border: 1px solid var(--color-border-subtle);
    border-radius: 12px;
    padding: 1.75rem;
    transition:
      border-color 0.2s,
      box-shadow 0.2s,
      transform 0.2s;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .feature-card:hover {
    border-color: rgba(212, 160, 23, 0.3);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
    transform: translateY(-2px);
  }

  .feature-card-accent {
    background: linear-gradient(135deg, rgba(107, 76, 0, 0.2) 0%, rgba(21, 21, 21, 1) 60%);
    border-color: rgba(212, 160, 23, 0.2);
  }

  .feature-icon {
    font-size: 1.75rem;
    line-height: 1;
  }

  .feature-title {
    font-size: 1.0625rem;
    font-weight: 600;
    color: var(--color-light);
  }

  .feature-desc {
    font-size: 0.875rem;
    color: var(--color-text);
    line-height: 1.65;
    flex: 1;
  }

  .feature-desc code {
    font-size: 0.8rem;
    color: var(--color-primary-light);
    background: rgba(212, 160, 23, 0.1);
    padding: 0.1em 0.35em;
    border-radius: 4px;
  }

  .feature-link {
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--color-primary);
    text-decoration: none;
    transition: color 0.15s;
    font-family: var(--font-mono);
  }

  .feature-link:hover {
    color: var(--color-primary-light);
  }

  .feature-stat {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-primary);
    font-family: var(--font-mono);
  }

  /* ============================================
     PHILOSOPHY
     ============================================ */
  .philosophy-section {
    background: var(--color-dark);
    border-top: 1px solid var(--color-border-subtle);
    border-bottom: 1px solid var(--color-border-subtle);
  }

  .philosophy-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  @media (max-width: 768px) {
    .philosophy-inner {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
  }

  .philosophy-lead {
    font-size: 1.0625rem;
    color: var(--color-text);
    margin-top: 1rem;
    margin-bottom: 2rem;
    line-height: 1.65;
  }

  .philosophy-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .philosophy-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
  }

  .philosophy-icon {
    font-size: 1.25rem;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }

  .philosophy-item div {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .philosophy-item strong {
    color: var(--color-light);
    font-size: 0.9375rem;
    font-weight: 600;
  }

  .philosophy-item span {
    font-size: 0.875rem;
    color: var(--color-text);
    line-height: 1.5;
  }

  .philosophy-item code {
    font-size: 0.8rem;
    color: var(--color-primary-light);
    background: rgba(212, 160, 23, 0.1);
    padding: 0.1em 0.35em;
    border-radius: 4px;
  }

  .stats-card {
    background: var(--color-body);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 2rem;
    position: sticky;
    top: 5rem;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 1.25rem 0;
  }

  .stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-primary);
    font-family: var(--font-mono);
    letter-spacing: -1px;
  }

  .stat-label {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .stat-divider {
    height: 1px;
    background: var(--color-border-subtle);
  }

  /* ============================================
     CTA SECTION
     ============================================ */
  .cta-section {
    background: var(--color-body);
  }

  .cta-box {
    position: relative;
    text-align: center;
    background: var(--color-dark);
    border: 1px solid var(--color-border);
    border-radius: 20px;
    padding: 4rem 2rem;
    overflow: hidden;
  }

  .cta-glow {
    position: absolute;
    top: -50%;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 300px;
    background: radial-gradient(ellipse, rgba(212, 160, 23, 0.1) 0%, transparent 70%);
    pointer-events: none;
  }

  .cta-title {
    font-size: clamp(1.75rem, 3.5vw, 2.75rem);
    font-weight: 700;
    color: var(--color-light);
    letter-spacing: -1px;
    margin-bottom: 1rem;
    position: relative;
  }

  .cta-desc {
    font-size: 1.0625rem;
    color: var(--color-text);
    max-width: 440px;
    margin: 0 auto 2rem;
    line-height: 1.65;
    position: relative;
  }

  .cta-install {
    display: flex;
    justify-content: center;
    margin-bottom: 1.75rem;
    position: relative;
  }

  .cta-install .install-box {
    max-width: 480px;
  }

  .cta-actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
    position: relative;
  }

  /* ============================================
     FOOTER
     ============================================ */
  .site-footer {
    background: var(--color-dark);
    border-top: 1px solid var(--color-border-subtle);
    padding: 2.5rem 0;
  }

  .footer-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    align-items: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .footer-brand {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
    color: var(--color-light);
  }

  .footer-logo-icon {
    font-size: 1.125rem;
  }

  .footer-logo-text {
    font-family: var(--font-mono);
  }

  .footer-links {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    flex-wrap: wrap;
  }

  .footer-link {
    padding: 0.25rem 0.625rem;
    font-size: 0.875rem;
    color: var(--color-text-muted);
    border-radius: 5px;
    transition:
      color 0.15s,
      background-color 0.15s;
  }

  .footer-link:hover {
    color: var(--color-light);
    background-color: rgba(255, 255, 255, 0.05);
  }

  .footer-copy {
    margin-left: auto;
    font-size: 0.8rem;
    color: var(--color-text-muted);
  }

  .footer-copy a {
    color: var(--color-primary);
    transition: color 0.15s;
  }

  .footer-copy a:hover {
    color: var(--color-primary-light);
  }

  @media (max-width: 640px) {
    .footer-inner {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .footer-copy {
      margin-left: 0;
    }
  }
</style>

<script>
  // Copy-to-clipboard for install buttons
  document.querySelectorAll(".install-copy").forEach((btn) => {
    btn.addEventListener("click", () => {
      const text = (btn as HTMLElement).dataset.copy;
      if (!text) return;
      navigator.clipboard.writeText(text).then(() => {
        btn.classList.add("copied");
        const originalHTML = btn.innerHTML;
        btn.innerHTML =
          '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"></polyline></svg>';
        setTimeout(() => {
          btn.classList.remove("copied");
          btn.innerHTML = originalHTML;
        }, 2000);
      });
    });
  });
</script>
